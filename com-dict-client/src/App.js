// Importing the React library to define and create React components
import React from "react";

// Importing the main styling for the application
import "./App.css";

// Importing the Routes component, which likely contains the application's routing configuration
import Routes from "./routes";

// Defining the main functional component named App
export default function App() {
  return <Routes />;
}
